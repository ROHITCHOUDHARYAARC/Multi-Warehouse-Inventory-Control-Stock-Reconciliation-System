package com.rohit.inventory.repository; import com.rohit.inventory.entity.WarehouseZone; import java.util.*; import org.springframework.data.jpa.repository.JpaRepository; public interface WarehouseZoneRepository extends JpaRepository<WarehouseZone,UUID>{List<WarehouseZone> findByWarehouseId(UUID warehouseId); Optional<WarehouseZone> findByWarehouseIdAndCode(UUID warehouseId,String code);}

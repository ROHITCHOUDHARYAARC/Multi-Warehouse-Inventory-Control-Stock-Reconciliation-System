package com.rohit.inventory.repository; import com.rohit.inventory.entity.Notification; import java.util.*; import java.time.Instant; import org.springframework.data.domain.*; import org.springframework.data.jpa.repository.*; public interface NotificationRepository extends JpaRepository<Notification,UUID>{Page<Notification> findByRecipientId(UUID recipient,Pageable page); Page<Notification> findByRecipientIdAndRead(UUID recipient,boolean read,Pageable page); long countByRecipientIdAndRead(UUID recipient,boolean read);}
